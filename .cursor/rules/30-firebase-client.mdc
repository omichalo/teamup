---
description: Usage client Firebase / Firestore côté application
globs: src/lib/firebase*.ts,src/lib/firebase/**/*,src/lib/client/**/*
alwaysApply: false
---

# Firebase Client Usage

## Firestore

- Quand une collection est manipulée, tu t'assures systématiquement que les bonnes règles sont positionnées. Tu n'ouvres que le strict minimum pour que cela fonctionne
